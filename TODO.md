# TODO

Индекс задач. Номера 15–31 — пункты из `docs/specs/2026-09-24-design.md` §5.

## Ближайшее
- [ ] **D1 · Деплой на do-ams3-claude-01 (188.166.91.129)** — docker compose рядом с tg-claude-bot, named volume,
  `.env` с токеном бота; после деплоя выставить `MR_POLL_INTERVAL=0` в tg-claude-bot, чтобы не было дублей.
- [ ] **D2 · Релиз v0.1.0** — push в origin и тег после ревью владельца; GitHub Actions соберёт образ в ghcr.io.

## v0.2 — «не пропустить» + «не пинговать руками»

Порядок взят из разбора агента 2026-09-24. Номера 15–31 — пункты спеки §5, **Q/N** — новые.

### Быстрые победы
- [x] **Q1 · Цикл опроса не должен умирать молча** — ошибка записи heartbeat сейчас роняет цикл опроса, и бот перестаёт присылать всё.
- [x] **Q2 · Сообщение «токен перестал работать»** — один раз при переходе в состояние auth (часть п.29).
- [ ] **Q3 · Команды только в личке** — `/mr /inbox /settings /accounts` в группах светят приватные MR; отредактированное сообщение роняет обработчик текста.

### Дальше
- [ ] **N1 · «Ждёт повторного ревью» по коммитам** — после моего changes requested или комментариев автор запушил коммиты (сменился head sha) → ход снова мой, даже без re-request. Риск — rebase/merge master.
- [ ] 15 · Напоминание: MR висит на моём ревью ≥ N дней (лимит на тик и верхняя граница, как у эскалации).
- [ ] 18 · Склейка всплеска: «Submit review» с 10 комментариями = одно уведомление.
- [ ] 19 · Фильтры шума: боты (renovate/dependabot), `/mute <project>`.
- [ ] 22 · Метки `blocker`/`hotfix` приходят и в тихие часы.
- [ ] 21 · `/watch <ссылка на MR>`.
- [ ] 29–30 · `/status` (последний опрос, rate-limit) и `/test` (образцы уведомлений).
- [ ] 20 · Вечерняя сводка «что ждёт твоего ответа».
- [ ] 17 · Пинг коллеге личным сообщением в Telegram + invite-ссылки — когда в инстансе будет 3–4 человека из команды.

### Идеи на потом
- [ ] **N2 · Командная картина** — кто у кого держит ход, `/load` (п.24), сводка в командный чат по согласию админа, без названий MR.
- [ ] **N3 · Умная сортировка `/inbox`** — время ожидания × сколько людей заблокировано × размер diff × blocker, с подписью «почему выше».

### Не делаем
- Вебхуки (нужен публичный адрес), LLM-саммари (зависимость от API), `/export`/`/import` и CI-фейлы — до реального запроса.

## v0.3 — команда и метрики
- [ ] 23 · Незакрытые треды: автор ответил — ждёт резолва
- [ ] 24 · `/load` — нагрузка ревьюеров
- [ ] 25 · `/stats` — личная статистика ревью
- [ ] 26 · Пятничный отчёт
- [ ] 27 · Draft → Ready как событие
- [ ] 28 · CI-фейл на моей ветке
- [ ] 31 · `/export` / `/import` конфига
